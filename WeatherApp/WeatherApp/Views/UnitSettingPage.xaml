<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WeatherApp.Views.UnitSettingPage" Title="Cài đặt đơn vị" BackgroundColor="{DynamicResource PageBackgroundColor}">
    <ContentPage.Content>
        <StackLayout>

        <Label Text="Nhiệt độ" Grid.Row="0" HorizontalOptions="Center" FontSize="Medium"/>
        <CollectionView x:Name="TemperaturePicker" Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Center"
                        SelectionMode="Single" BackgroundColor="White" WidthRequest="80" HeightRequest="40">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding Title}" TextColor="Black" FontSize="Medium" WidthRequest="40"
                               VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
        </CollectionView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>